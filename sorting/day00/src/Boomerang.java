public class Boomerang {

    public static int numberOfBoomerangs(int[][] points) {
        for(int i=0;i< points.length;i++){
            //for(int )

        }
        return 0;
    }

    public static double calcDist(int[] pointa, int[] pointb){ //calculates distance between two points
        double distance= Math.sqrt((pointa[0]-pointb[0])^2+(pointa[1]-pointb[1])^2);
        return distance;
    }
}

